#include <iostream>
#include <string>
using namespace std;

string evaluate(string expression) {
  // evaluate ()
  // evaluate *
  // evaluate /
  // evaluate +
  // evaluate -
  
  return expression;
}

int main() {
  string expression;
  cin >> expression;
  cout << evaluate(expression) << "\n";  
  return 0;
}
